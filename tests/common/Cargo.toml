[package]
name = "taffy_test_helpers"
version = "0.0.0"
edition = "2021"
description = "Internal test helpers for taffy's tests"
repository = "https://github.com/DioxusLabs/taffy"
categories = ["gui"]
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
taffy = { path = "../.." }
